# CMake Sphinx Example

An example of using CMake for managing multiple Sphinx documentation projects.
This project demonstrates how to automate Sphinx documentation builds with CMake, including automatic Python virtual environment setup and dependency management.

## Requirements

- CMake 3.15 or higher
- Python 3.x

